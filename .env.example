# =====================================
# cargohold - 開発環境用環境変数テンプレート
# =====================================
# 使用方法:
#   1. このファイルを .env にコピー
#   2. 各値を開発環境に合わせて設定（またはそのまま使用）
#   3. docker compose -f compose.yml -f compose.dev.yml up
#
# 注意: .env ファイルはgitignoreに含まれているため、コミットされません
# CI環境ではこのファイルをそのまま .env としてコピーして使用します

# =====================================
# データベース認証情報
# =====================================
# PostgreSQLデータベースのパスワード
POSTGRES_PASSWORD=cargohold_dev_password

# =====================================
# AWS/S3互換ストレージ認証情報
# =====================================
# SeaweedFS/MinIO用のアクセスキー
AWS_ACCESS_KEY_ID=minioadmin
# SeaweedFS/MinIO用のシークレットキー
AWS_SECRET_ACCESS_KEY=minioadmin_dev_password

# =====================================
# テストユーザー認証情報
# =====================================
# E2Eテスト用ユーザーのパスワード
AUTH_TESTUSER_PASSWORD=testuser_dev_password
