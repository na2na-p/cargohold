# ==============================================
# Git LFS 設定ファイル (.lfsconfig)
# ==============================================
# このファイルをリポジトリのルートに配置することで、
# Git LFS クライアントが cargohold サーバーを使用するように設定されます。
#
# 使い方:
#   1. このファイルをリポジトリのルートにコピー
#   2. URL を実際の cargohold サーバーの URL に変更（owner/repo を含む）
#   3. git lfs install を実行
#   4. .gitattributes で追跡するファイルパターンを設定
#
# 重要: URL はリポジトリ固有です（owner/repo をパスに含める必要があります）
#
# 参考: https://github.com/git-lfs/git-lfs/blob/main/docs/man/git-lfs-config.adoc

[lfs]
    # cargohold サーバーの URL（リポジトリ固有）
    # {owner} と {repo} を実際のリポジトリに合わせて変更してください
    # 例: owner=myorg, repo=myproject の場合
    url = https://cargohold.example.com/myorg/myproject/info/lfs

    # ローカル開発環境の場合
    # url = http://localhost:8080/myorg/myproject/info/lfs

    # 転送アダプター（basic のみサポート）
    # 通常は指定不要（デフォルト: basic）
    # transfer = basic

    # バッチリクエストのタイムアウト（秒）
    # 大容量ファイルを扱う場合は増やすことを推奨
    # activitytimeout = 30

    # 同時転送数
    # concurrenttransfers = 8
